function loadSlider() {

    var sliderList = [];
    var thumbList = [];

    for (var index = 0; index <= images.length; index++) {

        var slide = images[index];

        thumbList.push(renderThumb(slide));

    }
}

function renderThumb(slide) {

    console.log(slide);

}

function renderSlide() {


}